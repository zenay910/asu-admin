import { PageHeader } from '@/components/page-header'

export default function PartsSectionPage() {
  return (
    <PageHeader
      title="Parts"
      description="Parts inventory and compatibility — list UI ships in F2."
    />
  )
}

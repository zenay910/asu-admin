import { PageHeader } from '@/components/page-header'

export default function JobsSectionPage() {
  return (
    <PageHeader
      title="Jobs"
      description="Work orders and standardized forms — list UI ships in F3."
    />
  )
}

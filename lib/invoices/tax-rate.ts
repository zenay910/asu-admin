/** South Salt Lake local sales tax rate; applied in invoice total recomputation. */
export const TAX_RATE = 0.0765

/** Credit card surcharge rate applied to pre-fee total when payment method is credit card. */
export const CREDIT_CARD_SURCHARGE_RATE = 0.03
